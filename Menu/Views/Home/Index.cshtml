1 lines (43 sloc)  1.1 KB

<h1 id="home">Canteen Menu</h1>
<div class="jumbotron">
    <img src="~/img/drawing.png" id="logo" />
    <p class="lead">Canteen menu today</p>
    <button id="menu"> @Html.ActionLink("Menu", "Menus1", "Home")</button>
    <br />
    <br />
    <button id="tahvel">
        <a href="https://tahvel.edu.ee">Tahvel</a>
    </button> <a></a>
    <button id="tthk">
        <a href="https://tthk.ee">Kool</a>
    </button>
</div>

<div>
    @model IEnumerable<Menu.Models.Menu1>

    @{
        ViewBag.Title = "Menus1";
    }

    <h2>Menus1</h2>

    <table class="table">
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Name)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Cost)
            </th>
            <th></th>
        </tr>
        <tr>
            <th>
                @Html.ActionLink("Name", "Index", new { sortOrder = ViewBag.NameSortParm })
            </th>
            <th>
                First Name
            </th>
            <th>
                @Html.ActionLink("Cost", "Index", new { Nam = ViewBag.DateSortParm })
            </th>
            <th></th>
        </tr>

        @foreach (var item in Model)
        {
            <tr>
                <td>
                    @Html.DisplayFor(modelItem => item.Name)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.Cost)
                </td>
            </tr>
        }

    </table>

</div>